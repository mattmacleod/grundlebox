%h1 Welcome to #{ Grundlebox::Config::SiteTitle }'s website manager

= note "The most recently updated articles are shown below."

= render :partial => "admin/articles/table", :locals => {:articles => @articles }
